name: Security checks

on:
  push:
    branches: ["main"]
    paths:
      - "veespeech/**"
      - ".github/workflows/security.yml"
      - "pyproject.toml"
      - "poetry.lock"
  workflow_call:

permissions:
  contents: read

jobs:
  security:
    name: Security checks (Bandit & Safety)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: |
          pipx install poetry
          poetry --version

      - name: Install security tools
        run: |
          pipx install bandit safety

      - name: Run Bandit
        run: |
          pipx run bandit -r veespeech -c pyproject.toml

      - name: Export deps for Safety
        run: |
          pipx run poetry export -f requirements.txt --only dev --without-hashes -o requirements.txt

      - name: Run Safety
        run: |
          pipx run safety check -r requirements.txt --full-report
